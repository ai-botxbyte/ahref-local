# Build / deploy artifacts
.serverless/
.build_tag
__pycache__/
*.pyc
.venv/
venv/

# Downloaded / generated
downloaded_files/
*.log

# Local secrets (just in case)
.env
.env.*
proxies.txt
/tmp/proxies.txt

# K8s (internal)
k8s/

# Large test outputs
results_500.json
results_500.summary.json
serverless.yml
.serverless.yml

# Local-only master profile staging area (built by tools/open_chrome_profile.sh,
# uploaded to GitHub release by tools/upload_profile_release.sh — never commit)
master-profile/
master-profile.zip

# Vendored browser binary + extension (populated by tools/setup_vendor.sh,
# one-time setup — never commit, too large / has its own .git)
vendor/ungoogled-chromium/
vendor/ungoogled-chromium.tar.xz
vendor/cf-autoclick/
